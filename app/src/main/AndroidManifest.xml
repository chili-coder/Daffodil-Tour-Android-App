<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sohaghlab.diutour">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_diu_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_diu_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DIUTOUR"
        android:usesCleartextTraffic="true">
        <activity android:name=".page.UpdateActivity"></activity>
        <activity android:name=".page.ContractActivity" />
        <activity android:name=".page.PaymentActivity" />
        <activity android:name=".page.TourRegiActivity" />
        <activity android:name=".Post_View_Activity" />
        <activity android:name=".AllowActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>